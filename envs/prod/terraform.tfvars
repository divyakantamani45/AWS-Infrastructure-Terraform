# AWS region to deploy in
region = "us-east-1"

# EKS cluster name
cluster_name = "devops-eks-prod"

# VPC CIDR block
vpc_cidr = "10.0.0.0/16"

# App domain (must exist in your Route53 hosted zone)
domain_name = "app.devops.com"
